<dashboard version="1.1">
  <label>DMA Benchmark — tstats vs search</label>
  <description>Side-by-side comparison of |tstats (accelerated datamodel) vs |search (raw events) on the same dataset, plus per-datamodel acceleration health. Demonstrates the latency win from Data Model Acceleration.</description>

  <!-- Global time picker. Every panel's search reads $tp.earliest$ + $tp.latest$
       so the user can widen / narrow the demo window from one control. -->
  <fieldset submitButton="false" autoRun="true">
    <input type="time" token="tp" searchWhenChanged="true">
      <label>Time range</label>
      <default>
        <earliest>-24h@h</earliest>
        <latest>now</latest>
      </default>
    </input>
  </fieldset>

  <row>
    <panel>
      <title>Datamodel acceleration health</title>
      <table>
        <search>
          <query>| rest /servicesNS/-/-/data/models splunk_server=local
| search acceleration=1
| eval datamodel = title
| eval earliest_time = 'acceleration.earliest_time'
| eval cron = 'acceleration.cron_schedule'
| join type=outer datamodel [
    | rest /servicesNS/-/-/admin/summarization splunk_server=local
    | eval datamodel = mvindex(split(summary_id, "_"), -1)
    | stats values(size) as bytes_summary values(access_count) as access_count
      values(is_inprogress) as build_in_progress values(complete) as completeness
      by datamodel
  ]
| eval size_mb = round(bytes_summary/1024/1024, 2)
| eval completeness_pct = round(completeness*100, 1)
| table datamodel earliest_time cron size_mb completeness_pct build_in_progress access_count
| sort datamodel</query>
          <earliest>-15m</earliest>
          <latest>now</latest>
          <sampleRatio>1</sampleRatio>
        </search>
        <option name="drilldown">none</option>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>|tstats over the Change datamodel — milliseconds</title>
      <single>
        <search>
          <query>| tstats summariesonly=t count from datamodel=Change</query>
          <earliest>$tp.earliest$</earliest>
          <latest>$tp.latest$</latest>
        </search>
        <option name="drilldown">none</option>
        <option name="numberPrecision">0</option>
        <option name="underLabel">events via |tstats (accelerated)</option>
      </single>
    </panel>
    <panel>
      <title>|search on raw events — seconds</title>
      <single>
        <!-- IMPORTANT: do NOT prefix this query with the literal word
             `search`. Splunk auto-optimizes `search ... | stats count`
             into `tstats prestats=true ... WHERE (... search)` and the
             keyword `search` ends up as a raw-text-match term in the
             WHERE clause, which returns 0. Using the implicit search
             form (no leading keyword) skips this auto-optimizer path. -->
        <search>
          <query>index=main sourcetype=aws:cloudtrail
| stats count</query>
          <earliest>$tp.earliest$</earliest>
          <latest>$tp.latest$</latest>
        </search>
        <option name="drilldown">none</option>
        <option name="numberPrecision">0</option>
        <option name="underLabel">events via raw |search</option>
      </single>
    </panel>
  </row>

  <row>
    <panel>
      <title>Top CloudTrail actions (Change datamodel, accelerated)</title>
      <chart>
        <search>
          <query>| tstats summariesonly=t count from datamodel=Change by All_Changes.action, All_Changes.object_category
| rename All_Changes.action AS action, All_Changes.object_category AS object_category
| sort -count
| head 20</query>
          <earliest>$tp.earliest$</earliest>
          <latest>$tp.latest$</latest>
        </search>
        <option name="charting.chart">bar</option>
        <option name="charting.legend.placement">right</option>
        <option name="charting.axisTitleY.text">events</option>
        <option name="drilldown">none</option>
      </chart>
    </panel>
    <panel>
      <title>Authentication events by action (accelerated)</title>
      <chart>
        <search>
          <query>| tstats summariesonly=t count from datamodel=Authentication by Authentication.action, Authentication.app
| rename Authentication.action AS action, Authentication.app AS app
| sort -count
| head 20</query>
          <earliest>$tp.earliest$</earliest>
          <latest>$tp.latest$</latest>
        </search>
        <option name="charting.chart">bar</option>
        <option name="charting.legend.placement">right</option>
        <option name="charting.axisTitleY.text">events</option>
        <option name="drilldown">none</option>
      </chart>
    </panel>
  </row>

  <row>
    <panel>
      <title>How to read this dashboard</title>
      <html>
        <body>
          <h3>What you're looking at</h3>
          <p>This POC enables Data Model Acceleration (DMA) on the
          <b>Change</b>, <b>Authentication</b>, and <b>Network_Traffic</b> CIM
          datamodels. Every 5 minutes Splunk pre-summarizes the last 7 days of
          matching events into tsidx files. <code>|tstats</code> reads those
          summary files directly, skipping the raw-event scan that
          <code>|search</code> performs.</p>

          <h3>Why DMA matters</h3>
          <ul>
            <li><b>Latency.</b> <code>|tstats</code> returns in tens of
            milliseconds even over millions of events. <code>|search</code>
            scales linearly with event volume.</li>
            <li><b>Detections.</b> Splunk ES correlation searches and most
            ESCU detections run as <code>|tstats</code>-style queries against
            CIM datamodels. Without acceleration the detection schedule
            falls behind real-time.</li>
            <li><b>Cost.</b> Accelerated datamodels reduce search-head CPU
            and search-peer disk I/O. The trade-off is index storage
            (tsidx summaries are typically ~10–20% of raw event size).</li>
          </ul>

          <h3>Notes</h3>
          <ul>
            <li>The two count panels both read the same time range from the
              picker at the top — change it to expand or narrow the demo
              window. "Last 24 hours" is a good default for our event volume;
              "Last 7 days" exercises the full DMA backfill window.</li>
            <li>Both <code>|tstats</code> count panels use
              <code>summariesonly=t</code>, so they only read from the
              accelerated summaries. If you don't see numbers, the
              acceleration build hasn't run yet — see the health table at the
              top of the page for state.</li>
            <li>The acceleration-health row queries Splunk's REST APIs and
              isn't governed by the time picker.</li>
          </ul>
        </body>
      </html>
    </panel>
  </row>
</dashboard>
